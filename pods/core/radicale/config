[server]
hosts = 0.0.0.0:5232

[auth]
type = htpasswd
htpasswd_filename = /data/users
htpasswd_encryption = sha512

[storage]
filesystem_folder = /data/collections

[web]
type = internal

[rights]
type = owner_only
