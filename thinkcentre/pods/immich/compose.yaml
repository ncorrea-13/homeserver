name: immich

services:
  immich-server:
    container_name: immich_server
    image: ghcr.io/immich-app/immich-server:${IMMICH_VERSION}
    ports:
      - "${IMMICH_PORT}:2283"
    volumes:
      - ${UPLOAD_LOCATION}:/data:Z
      - /etc/localtime:/etc/localtime:ro
    env_file:
      - .env
    restart: always
    # extends: # uncomment for hardware-accelerated transcoding, see hwaccel.transcoding.yml
    #   file: hwaccel.transcoding.yml
    #   service: cpu # one of [nvenc, quicksync, rkmpp, vaapi, vaapi-wsl, cpu]

  immich-machine-learning:
    container_name: immich_machine_learning
    image: ghcr.io/immich-app/immich-machine-learning:${IMMICH_VERSION}
    volumes:
      - model-cache:/cache:Z
    restart: always
    # extends: # uncomment for hardware-accelerated ML, see hwaccel.ml.yml
    #   file: hwaccel.ml.yml
    #   service: cpu # one of [armnn, cuda, rocm, openvino, openvino-wsl, rknn, cpu]

  redis:
    container_name: immich_redis
    image: docker.io/valkey/valkey:9@sha256:3b55fbaa0cd93cf0d9d961f405e4dfcc70efe325e2d84da207a0a8e6d8fde4f9

  database:
    container_name: immich_postgres
    image: ghcr.io/immich-app/postgres:14-vectorchord0.4.3-pgvectors0.2.0@sha256:bcf63357191b76a916ae5eb93464d65c07511da41e3bf7a8416db519b40b1c23
    environment:
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_USER: ${DB_USERNAME}
      POSTGRES_DB: ${DB_DATABASE_NAME}
      POSTGRES_INITDB_ARGS: "--data-checksums"
    volumes:
      - ${DB_DATA_LOCATION}:/var/lib/postgresql/data:Z
    shm_size: 128mb
    restart: always

volumes:
  model-cache:
